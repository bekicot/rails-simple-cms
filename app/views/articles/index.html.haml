%h1 Index Artikel

%table
  %tr
    %th Title
    %th Text
    %th Author
    %th Category
    %th Tags
  - @article.each do |post|

    %tr
      %td= post.title 
      %td= post.body
      %td= post.user.first_name
      %td= post.category.name
      %td= post.tags.join(', ')
      %td= link_to 'Show', article_path(post)
      %td= link_to 'Edit', edit_article_path(post)
      %td= link_to 'Delete Article', article_path(post), method: :delete, data: { confirm: 'Are You Sure ?' }
